# Copy to infra/.env on the server and fill with long random values
# (openssl rand -hex 24). Compose refuses to start without them.
CLICKHOUSE_ADMIN_PASSWORD=
APP_PASSWORD=
LOADER_PASSWORD=
ORIGIN=https://unquote.dwainosaur.com
